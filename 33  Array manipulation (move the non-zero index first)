# Array manipulation (move the non-zero index first)

arr = [0, 1, 0, 3, 12]

idx = 0

for i in range(len(arr)):
    if arr[i] != 0:
        arr[idx] = arr[i]
        idx += 1

for i in range(idx, len(arr)):
    arr[i] = 0

print(arr)

o/p:[1, 3, 12, 0, 0]
